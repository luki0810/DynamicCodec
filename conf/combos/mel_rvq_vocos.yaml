# Auto-generated by bash.sh; safe to edit if you want combo-specific tweaks.
$include:
  - ../input/melspec.yaml
  - ../model/encoder/mel.yaml
  - ../model/decoder/mel.yaml
  - ../model/quantizer/rvq.yaml
  - ../model/vocoder/vocos.yaml

# component identity (consumed by model/build.py:build_model)
input_format: melspec
encoder: mel
quantizer: rvq
decoder: mel
vocoder: vocos

# inference runtime
sample_rate: 48000
batch_size: 1
device: cuda:0
ssl_model.device: cuda:0
seed: 42
resume: false
